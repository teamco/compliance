# db-supabase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build db-supabase` to build the library.

## Running unit tests

Run `nx test db-supabase` to execute the unit tests via [Vitest](https://vitest.dev/).
