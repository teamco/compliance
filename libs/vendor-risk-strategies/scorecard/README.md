# scorecard

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scorecard` to build the library.

## Running unit tests

Run `nx test scorecard` to execute the unit tests via [Vitest](https://vitest.dev/).
